# Root example env: copy into `backend/.env` or use at repo root for guidance
# Set a production MongoDB Atlas SRV URI here (leave empty to use local fallback if configured)
# Example SRV form (Atlas):
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.attswcr.mongodb.net/qr-menu?retryWrites=true&w=majority
MONGODB_URI=

# Optional local fallback — set this if you want the server to try it automatically
# when an Atlas SRV DNS lookup fails:
# LOCAL_MONGODB_URI=mongodb://localhost:27017/qr-menu
LOCAL_MONGODB_URI=

PORT=5000
