### MongoDB connection string to use for production (Atlas or SRV form)
# Example SRV (Atlas):
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.attswcr.mongodb.net/qr-menu?retryWrites=true&w=majority
MONGODB_URI=

### Optional: local fallback used only when SRV lookup fails and this is provided
# Example: LOCAL_MONGODB_URI=mongodb://localhost:27017/qr-menu
LOCAL_MONGODB_URI=

PORT=5000
